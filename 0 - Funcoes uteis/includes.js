const tech = 'Node.js';

const includes = tech.includes('ode', posicao[]);

console.log({includes});


// A função includes() é um método de string em JavaScript que verifica se uma string contém outra string.
// Ela retorna true se a string contém a substring especificada e false caso contrário.
// O método é sensível a maiúsculas e minúsculas, ou seja, ele diferencia entre letras maiúsculas e minúsculas.
// Sua sintaxe é: string.includes(substring).
// Onde 'string' é a string original que está sendo verificada e 'substring' é a string que estamos procurando.
// Se 'substring' estiver contida na 'string', a função retorna true; caso contrário, retorna false.
